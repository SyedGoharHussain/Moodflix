# Node
node_modules/
npm-debug.log
yarn-error.log
yarn-debug.log

# Build Excludes
client/dist/
client/build/
server/build/
server/dist/

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/
env/
ENV/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDEs and editors
.vscode/
.idea/
*.swp
*.swo

# Sensitive files
server/firebase_credentials.json
firebase_credentials.json

# Large ML artifacts (regenerate with server/ml/train_transformer.py)
server/ml/models/transformer/model.safetensors

# Raw training datasets downloaded from the web (MovieLens / TMDB / IMDB /
# GoEmotions / dair-ai). ~3.6 GB — never commit. build_dataset.py reads these
# locally and falls back to HuggingFace download when absent.
/models/

# Local scratch / tooling from the GPU retrain + report-update work
/_*
torch-*.whl
client/_*.mjs
